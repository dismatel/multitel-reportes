# Azure Functions core
azure-functions==1.17.0

# Azure SDK
azure-identity==1.18.0
azure-storage-blob==12.23.0
azure-keyvault-secrets==4.8.0

# Durable Functions — instalado desde GitHub por incompatibilidad PyPI con Python 3.11
# La versión en PyPI requiere Python >=3.13
git+https://github.com/Azure/azure-functions-durable-python.git@v1.2.2

# Microsoft Graph / MSAL
msal==1.31.0
PyJWT==2.9.0
msgraph-core==1.3.8

# HTTP clients
requests==2.32.3
httpx==0.27.2
aiohttp==3.10.10

# PPTX generation
python-pptx==1.0.2
Pillow==10.4.0

# PDF generation — FIX A-3: reemplaza LibreOffice con Python puro
reportlab==4.1.0

# Validation & serialization
pydantic==2.9.2

# Cryptography
cryptography==43.0.3

# Google Play Integrity
google-auth==2.35.0

# NOTE: google-auth-httplib2 eliminado — no se usa en ninguna function (FIX B-6)
# NOTE: azure-mgmt-resource eliminado — aumentaba cold start innecesariamente
